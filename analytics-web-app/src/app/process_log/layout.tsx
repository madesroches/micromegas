import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Process Log',
}

export default function ProcessLogLayout({ children }: { children: React.ReactNode }) {
  return children
}
