import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Process Trace',
}

export default function ProcessTraceLayout({ children }: { children: React.ReactNode }) {
  return children
}
