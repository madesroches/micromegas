import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Process Metrics',
}

export default function ProcessMetricsLayout({ children }: { children: React.ReactNode }) {
  return children
}
