import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Performance Analysis',
}

export default function PerformanceAnalysisLayout({ children }: { children: React.ReactNode }) {
  return children
}
