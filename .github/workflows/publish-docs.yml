name: Publish Docs

on:
  push:
    branches:
      - main
      - analytics  # temporary: test goatcounter integration
  # Also run on any branch when the workflow config changes
  pull_request:
    paths:
      - '.github/workflows/publish-docs.yml'
      - 'welcome/**'
      - 'mkdocs/**'
      - 'doc/**'

jobs:
  build-and-deploy-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable

    - name: Install mold linker
      run: |
        sudo apt-get update
        sudo apt-get install -y mold

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Build Rust documentation
      run: |
        echo "🦀 Building Rust documentation..."
        cargo doc --workspace --no-deps --verbose
        echo "📁 Checking generated documentation..."
        ls -la target/doc/ | head -10
        echo "📝 Documentation index:"
        find target/doc -name "index.html" | head -5
      working-directory: rust/

    - name: Install MkDocs and dependencies
      run: pip install -r mkdocs/docs-requirements.txt
      
    - name: Build welcome page
      run: |
        cd welcome
        yarn install
        yarn build

    - name: Prepare staging directory
      run: |
        mkdir -p public_docs/rustdoc
        
        # Check if Rust docs were built
        if [ -d "rust/target/doc" ]; then
          echo "✅ Rust documentation found"
          cp -r rust/target/doc/* public_docs/rustdoc/
          ls -la public_docs/rustdoc/ | head -10
        else
          echo "❌ Rust documentation not found"
          echo "Creating placeholder index for rustdoc"
          cat > public_docs/rustdoc/index.html << 'RUSTDOC_EOF'
        <!DOCTYPE html>
        <html>
        <head>
            <title>Rust Documentation</title>
        </head>
        <body>
            <h1>Rust Documentation Build Failed</h1>
            <p>The Rust documentation failed to build. Please check the build logs.</p>
            <p><a href="../">← Back to main documentation</a></p>
        </body>
        </html>
        RUSTDOC_EOF
        fi
        
        # Build MkDocs documentation
        mkdocs build --config-file mkdocs/mkdocs.yml --site-dir $PWD/public_docs/docs
        
        # Copy legacy doc files for existing links
        mkdir -p public_docs/doc
        cp -r doc/* public_docs/doc/

        # Build high-frequency observability presentation
        cd doc/high-frequency-observability
        npm install
        npm run build:standalone
        cd ../..
        mkdir -p public_docs/high-frequency-observability
        cp doc/high-frequency-observability/dist/presentation-inline.html public_docs/high-frequency-observability/index.html
        cp -r doc/high-frequency-observability/dist/*.{png,svg} public_docs/high-frequency-observability/ 2>/dev/null || echo "No media files to copy (already inlined)"

        # Build unified observability for games presentation
        cd doc/unified-observability-for-games
        yarn install
        yarn build:standalone
        cd ../..
        mkdir -p public_docs/unified-observability-for-games
        cp doc/unified-observability-for-games/dist/presentation-inline.html public_docs/unified-observability-for-games/index.html
        cp -r doc/unified-observability-for-games/dist/*.{png,svg} public_docs/unified-observability-for-games/ 2>/dev/null || echo "No media files to copy (already inlined)"
        
        # Copy welcome page build output as the root index
        cp -r welcome/dist/* public_docs/
        
        # Disable Jekyll
        touch public_docs/.nojekyll

    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/analytics'
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public_docs
        publish_branch: gh-pages
        force_orphan: true
